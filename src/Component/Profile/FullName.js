import React from 'react'

function FullName() {
    const name= "Oumayma"
    const familyName= "Sdiri"
  return (
    <div>
    <h1> {`${name} ${familyName}`}</h1>
    </div>
  )
}

export default FullName